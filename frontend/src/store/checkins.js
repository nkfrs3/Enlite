import { csrfFetch } from "./csrf";




const createCheckIn = (data) => async (dispatch) => {
  // const {id,}
}

const initialState = [];

const checkInsReducer = (state = initialState, action) => {
  switch(action.type){


    default: return state;
  }
}


export default checkInsReducer;
